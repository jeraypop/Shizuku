<?xml version="1.0" encoding="utf-8"?>
<manifest
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- This manifest is designed to be merged into a host app (Shizuku as a library).
         Application-level attributes (icon/label/theme/backup...) are intentionally NOT
         set here so they never conflict with the host app; the standalone app shell (:app)
         provides them. -->
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.INTERNET" />
    <!-- Android 17 (SDK 37): local network access is gated behind these permissions;
         required for mDNS discovery used by wireless ADB pairing and for the
         privileged server to reach loopback. -->
    <uses-permission android:name="android.permission.USE_LOOPBACK_INTERFACE" />
    <uses-permission android:name="android.permission.ACCESS_LOCAL_NETWORK" />
    <!-- Android 16 gates local-network discovery behind NEARBY_WIFI_DEVICES. -->
    <uses-permission
        android:name="android.permission.NEARBY_WIFI_DEVICES"
        android:usesPermissionFlags="neverForLocation"
        tools:targetApi="33" />
    <!-- Needed to receive mDNS multicast for wireless ADB service discovery. -->
    <uses-permission android:name="android.permission.CHANGE_WIFI_MULTICAST_STATE" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.REQUEST_IGNORE_BATTERY_OPTIMIZATIONS" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_SPECIAL_USE" />
    <uses-permission android:name="android.permission.REQUEST_DELETE_PACKAGES" />
    <uses-permission android:name="android.permission.REQUEST_INSTALL_PACKAGES" />

    <uses-permission
        android:name="android.permission.WRITE_SECURE_SETTINGS"
        tools:ignore="ProtectedPermissions" />

    <uses-feature
        android:name="android.software.leanback"
        android:required="false" />
    <uses-feature
        android:name="android.hardware.touchscreen"
        android:required="false" />

    <!-- Permission groups are globally unique and owned by the first installer;
         parameterize so the host app and the standalone app can coexist. -->
    <permission-group
        android:name="${shizukuApplicationId}.permission-group.API"
        android:description="@string/permission_group_description"
        android:icon="@drawable/ic_system_icon"
        android:label="@string/permission_group_label" />

    <permission
        android:name="moe.shizuku.manager.permission.MANAGER"
        android:protectionLevel="signature" />
    <uses-permission android:name="moe.shizuku.manager.permission.MANAGER" />

    <permission
        android:name="moe.shizuku.manager.permission.API_V23"
        android:description="@string/permission_description"
        android:icon="@drawable/ic_system_icon"
        android:label="@string/permission_label"
        android:permissionGroup="${shizukuApplicationId}.permission-group.API"
        android:protectionLevel="dangerous" />
    <uses-permission
        android:name="moe.shizuku.manager.permission.API_V23"
        tools:node="remove" />

    <permission
        android:name="${shizukuApplicationId}.DYNAMIC_RECEIVER_NOT_EXPORTED_PERMISSION"
        android:protectionLevel="signature"
        tools:node="remove" />
    <uses-permission
        android:name="${shizukuApplicationId}.DYNAMIC_RECEIVER_NOT_EXPORTED_PERMISSION"
        tools:node="remove" />

    <queries>
        <package android:name="moe.shizuku.privileged.api" />
    </queries>

    <application
        tools:ignore="GoogleAppIndexingWarning"
        tools:remove="android:appComponentFactory"
        tools:targetApi="33">

        <activity
            android:name=".MainActivity"
            android:banner="@mipmap/banner"
            android:exported="true"
            android:theme="@style/AppTheme">
        </activity>
        <activity
            android:name=".management.ApplicationManagementActivity"
            android:label="@string/home_app_management_title"
            android:theme="@style/AppTheme" />
        <activity
            android:name=".adb.AdbPairingTutorialActivity"
            android:label="@string/adb_pairing"
            android:theme="@style/AppTheme" />
        <activity
            android:name=".shell.ShellTutorialActivity"
            android:label="@string/home_terminal_title"
            android:theme="@style/AppTheme" />
        <activity
            android:name=".stealth.StealthTutorialActivity"
            android:label="@string/home_stealth_title"
            android:theme="@style/AppTheme"
            android:windowSoftInputMode="adjustResize" />
        <activity
            android:name=".settings.SettingsActivity"
            android:exported="false"
            android:label="@string/settings_title"
            android:theme="@style/AppTheme">
            <intent-filter>
                <action android:name="android.intent.action.APPLICATION_PREFERENCES" />
            </intent-filter>
        </activity>
        <activity
            android:name=".starter.StarterActivity"
            android:label="@string/starter"
            android:theme="@style/AppTheme" />
        <activity
            android:name=".settings.BugReportDialogActivity"
            android:theme="@style/Theme.App.DialogHost"
            android:taskAffinity=""
            android:excludeFromRecents="true"
            android:launchMode="singleTask"
            android:exported="false" />
        <activity
            android:name=".authorization.RequestPermissionActivity"
            android:directBootAware="true"
            android:excludeFromRecents="true"
            android:exported="true"
            android:permission="android.permission.INTERACT_ACROSS_USERS_FULL"
            android:theme="@style/GrantPermissions">
            <intent-filter>
                <action android:name="${shizukuApplicationId}.intent.action.REQUEST_PERMISSION" />

                <category android:name="android.intent.category.DEFAULT" />
            </intent-filter>
        </activity>
        <activity
            android:name=".legacy.ShellRequestHandlerActivity"
            android:directBootAware="true"
            android:excludeFromRecents="true"
            android:exported="true"
            android:theme="@style/GrantPermissions">
            <intent-filter>
                <action android:name="rikka.shizuku.intent.action.REQUEST_BINDER" />

                <category android:name="android.intent.category.DEFAULT" />
            </intent-filter>
        </activity>
        <activity
            android:name=".legacy.LegacyIsNotSupportedActivity"
            android:excludeFromRecents="true"
            android:exported="true"
            android:permission="moe.shizuku.manager.permission.API"
            android:theme="@style/GrantPermissions">
            <intent-filter>
                <action android:name="${shizukuApplicationId}.intent.action.REQUEST_AUTHORIZATION" />

                <category android:name="android.intent.category.DEFAULT" />
            </intent-filter>
        </activity>

        <service
            android:name=".adb.AdbPairingService"
            android:enabled="true"
            android:exported="false"
            android:foregroundServiceType="shortService" />

        <service
            android:name=".adb.AdbPairingAccessibilityService"
            android:permission="android.permission.BIND_ACCESSIBILITY_SERVICE"
            android:exported="true">
            <intent-filter>
                <action android:name="android.accessibilityservice.AccessibilityService" />
            </intent-filter>
            <meta-data
                android:name="android.accessibilityservice"
                android:resource="@xml/accessibility_service_config" />
        </service>

        <service
            android:name=".service.WatchdogService"
            android:exported="false"
            android:foregroundServiceType="specialUse">
            <property
                android:name="android.app.PROPERTY_SPECIAL_USE_FGS_SUBTYPE"
                android:value="Many apps rely on this app's API service for core functionality.
                    The API service can stop unexpectedly for various reasons (e.g., when connecting to Android Auto over USB).
                    This prevents the other apps from functioning properly.
                    The watchdog service would ensure the API is always available for the apps to use.
                    The watchdog service uses minimal resources and battery. There is no constant polling; it only reacts when the API service stops unexpectedly.
                    The watchdog service is opt-in and toggleable via settings, so users can choose to disable it at any time if not desired." />
        </service>

        <receiver
            android:name=".receiver.BinderRequestReceiver"
            android:directBootAware="true"
            android:enabled="true"
            android:exported="true"
            tools:ignore="ExportedReceiver">
            <intent-filter>
                <action android:name="rikka.shizuku.intent.action.REQUEST_BINDER" />
            </intent-filter>
        </receiver>

        <receiver
            android:name=".receiver.BootCompleteReceiver"
            android:enabled="false"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
            </intent-filter>
        </receiver>

        <receiver
            android:name=".receiver.ManualStartReceiver"
            android:enabled="true"
            android:exported="true">
            <intent-filter>
                <action android:name="${shizukuApplicationId}.START" />
            </intent-filter>
        </receiver>

        <receiver
            android:name=".receiver.ManualStopReceiver"
            android:enabled="true"
            android:exported="true">
            <intent-filter>
                <action android:name="${shizukuApplicationId}.STOP" />
            </intent-filter>
        </receiver>

        <receiver android:name=".receiver.NotifAttemptReceiver" />
        <receiver android:name=".receiver.NotifCancelReceiver" />
        <receiver android:name=".receiver.NotifRestoreReceiver" />

        <provider
            android:name=".SZKFileProvider"
            android:authorities="${shizukuApplicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>
        <!--为了防止 从shizuku-api切换到本库时，没有及时移出对应的provider导致的连不上 -->
        <provider
            android:name="rikka.shizuku.ShizukuProvider"
            tools:node="remove" />

        <provider
            android:name=".ShizukuManagerProvider"
            android:authorities="${shizukuApplicationId}.shizuku"
            android:directBootAware="true"
            android:enabled="true"
            android:exported="true"
            android:multiprocess="false"
            android:permission="android.permission.INTERACT_ACROSS_USERS_FULL" />

        <!-- Initializes the manager code in the host process without touching the
             host's Application class. -->
        <provider
            android:name="androidx.startup.InitializationProvider"
            android:authorities="${shizukuApplicationId}.androidx-startup"
            android:exported="false"
            tools:node="merge">
            <meta-data
                android:name="moe.shizuku.manager.ShizukuInitializer"
                android:value="androidx.startup" />
        </provider>
    </application>

</manifest>
